import * as React from 'react';

interface IDailyInformationsProps {
    title: string
    information: number
}

const DailyInformations: React.FunctionComponent<IDailyInformationsProps> = (props) => {
  return (
      <div></div>
  );
};


export default DailyInformations;