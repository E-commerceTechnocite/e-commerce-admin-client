import * as React from 'react';

interface IActionStockProps {
}

const ActionStock: React.FunctionComponent<IActionStockProps> = (props) => {
  return (
      <div className="action-stock">action stock</div>
  )
};

export default ActionStock;
