import * as React from 'react';

interface IActionCustomerProps {
}

const ActionCustomer: React.FunctionComponent<IActionCustomerProps> = (props) => {
  return <div>HENLO</div>
};

export default ActionCustomer;
